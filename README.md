Create new folder for each cpp file/project  
Use ctrl+shift+b to build current cpp files  
Then navigate into the location of current cpp file through terminal and run `./file-name`(without file extension)
